apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "netflix-clone-chart.fullname" . }}
  labels:
    app: {{ include "netflix-clone-chart.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "netflix-clone-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "netflix-clone-chart.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80
          resources:
            {{- toYaml .Values.resources | nindent 12 }}